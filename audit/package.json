{
  "name": "responsive-tester-audit",
  "private": true,
  "version": "0.5.0",
  "description": "Live-session sidecar deps for the Responsive Tester desktop app. Kept separate from the root package so the desktop bundle's first-run install only pulls Playwright, not Vite + Tauri build tooling.",
  "type": "module",
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
